rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Phone+Password role validation and security checks are enforced
    // by backend service logic and data model constraints.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

